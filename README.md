# ChainReactor
Chain Reactor is a high-utility automation system designed to bridge the gap between a static UI and a live terminal session. It is built to be minimalistic, crystal clear, and smart, allowing you to execute complex command pipelines without losing context or environment state.

🔗 CHAIN REACTOR (v2026.1)
A Persistent Terminal Workspace for Precise Automation.

🚀 Overview
Chain Reactor is a specialized automation environment designed for users who need to bridge the gap between manual terminal commands and full Python automation. It maintains directory persistence and session state across multiple steps.

🛠️ Key Features
8-Bit Workspace: A minimalistic, high-contrast interface for maximum focus.
The Sequencer (o1, o2...): Select and order your commands dynamically.
Smart-List Injection: Pass unquoted lists like cat [file1.py, file2.py] and watch the engine unroll them.
Precision Selection: Copy specific strings (UUIDs, paths) directly from interactive text areas.
Script Architect: Export your live UI session into a standalone automate.py script with one click.

🏁 Quick Start
Run the system:

Bash

python chain_reactor.py
Build your chain of commands.

Use 🧩 to pipe output from one step into the next via {row}.

Select the order of execution and hit 🚀 RUN CHAIN.
